# 历史文档

本目录存放 ZhouTomo 重构前或重构过程中的历史说明、完成总结和旧实现文档。

这些文件可能包含已经失效的内容，例如：

- 旧的 `server_fastapi.py`、`ports_temscript.py`、`agent_client.py` 顶层路径；
- 旧的 `view/`、`model/`、`src/`、`autofocus/` package；
- 旧默认端口；
- 已删除的 compatibility alias；
- 已被当前 `zhoutomo_*` namespace 替代的 import 示例。

因此：

> **`docs/legacy/` 只用于追溯历史，不作为当前开发、API 或架构规范。**

当前有效文档请从 [`../README.md`](../README.md) 开始阅读。
