# AgentClient依赖包
# 用于远程电脑的显微镜代理客户端

# HTTP客户端
aiohttp>=3.8.0

# WebSocket客户端
websockets>=10.0

# 异步支持（Python 3.7+内置，但为了兼容性列出）
# asyncio - 内置模块

# 类型提示支持（Python 3.5+内置）
# typing - 内置模块

# 日志（内置模块）
# logging - 内置模块

# JSON处理（内置模块）
# json - 内置模块

# 时间处理（内置模块）
# time - 内置模块

# URL处理（内置模块）
# urllib.parse - 内置模块

# 上下文管理器（内置模块）
# contextlib - 内置模块

# 可选：开发依赖
# pytest>=6.0.0          # 单元测试框架
# pytest-asyncio>=0.18.0 # 异步测试支持
# pytest-mock>=3.6.0     # Mock支持
